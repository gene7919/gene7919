from setuptools import setup, find_packages

setup(
    name="gene7919",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "web3>=6.0.0",
        "python-dotenv>=1.0.0",
    ],
)
